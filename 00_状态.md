---
status: 持续运行
status_since: 2026-09-06
owner: "jnq & AI"
entry: "SOP.md"
summary: "Pi CLI 汉化维护：适配 v0.87.0，i18n/ 七源共 530 条翻译事实（CLI 476 + 插件命令简介 36 + 插件 UI 18）+ 3 条用户授权行为补丁（4 个锚点：欢迎页空壳先行 + F1 todo 结算残留提醒 + F2 面板结算清理；editor 边框与 dock 两行已于 powerline 0.17.2 退役——上游已原生实现）；六条维护线（A 本体 / B 简介 / C 渲染文案 / D 懒人包安装 / E 懒人包导出 / F 插件功能补丁）；含 DoD 五项冒烟验收 + F 线四步验收"
next: "跟踪上游发版；升级 pi 后先按 SKILL.md A1 检查 .zh-backup 残留再重打 A 线；插件升级后跑 scripts/scan_plugin_commands.py --check 补译命令简介，跑 scripts/apply_plugin_ui.sh --check 审查欢迎页文案与行为补丁漂移，跑 scripts/patch_plugin_ui.py --dict i18n/plugin-logic.json --check 审查 F 线功能补丁漂移，跑 node scripts/probe_todo_overlay_cleanup.mjs 复验 F2；probe_editor_border.py 与 probe_dock_rows.py 已转为「上游原生行为是否回退」的回归哨兵（两条 C 线补丁已退役）；本机自研扩展或协同配置有变动时跑 scripts/export_bundle.sh 重新导出并跑 check_bundle.sh 确认无漂移"
updated: 2026-09-22
---

> 本项目采用 PKM 时空胶囊方法论（**零工具档：仅约定，未引入 task_status.py**）。
> 本卡记录维护基线；操作规程见 `SOP.md`，维护红线与完成定义见 `AGENTS.md`。
>
> 因未引入工具，`source_hash` 字段省略 —— 它由 `task_status.py` 计算，属"约定 + 工具"档位的能力。
> 建立：`qPKM治理` @ 2026-09-11

## 当前基线

| 项 | 值 |
| :--- | :--- |
| 适配版本 | Pi `0.87.0`（`scripts/patch_engine.py` → `SUPPORTED_VERSIONS` = `["0.85.1", "0.86.1", "0.87.0"]`） |
| 翻译事实源 | `i18n/` 七源：`cli.json` / `commands.json` / `keybindings.json` / `settings.json` / `ui.json` / `plugins.json` / `plugin-ui.json` |
| 条目规模 | 共 530 条翻译事实（CLI 汉化 476：keybindings 90 · settings 38 · commands 24 · cli 56 · ui 268；插件命令简介 36；插件 UI 文案 18），另有 3 条用户授权行为补丁（共 4 个锚点：C 线 1 + F 线 3） |
| 0.87.0 增量适配（2026-09-22） | 上游 0.86.1 → 0.87.0 的 Breaking Changes 全部在 SDK / 扩展 API 面，UI 面零新增；普查确认命令 24/24、设置项 38/38 全命中，无需补译 |
| UI 覆盖面补齐（2026-09-22，用户拍板「全面补齐 TUI 提示」） | ① `/hotkeys` 命令表格（Keyboard Shortcuts / Navigation / Editing / Other 四段共 41 条）从完全未汉化到全量汉化；② 各选择器与状态提示 100+ 条（/model 范围提示、会话选择器排序、tree 选择器、首次设置向导、登录对话框、config-selector、thinking 级别描述等）；③ `keybindings.json` 由 62 条补齐到上游 `tui.*` + `app.*` 全量 90 条（**注意：keybindings 的 description 目前无任何 UI 消费者**——`KeybindingsManager` 只暴露 `getEffectiveConfig`，`/hotkeys` 表格文案是渲染期硬编码的——补齐仅为字典与上游定义一一对应）；④ 清理 7 条无命中死条目（如 `Press Ctrl+P to cycle models`，上游已删改）。**已知未覆盖面**：`/bug` 交互对话框正文、缓存预热的会话记录通知、`/session` 诊断行、`/debug` 输出、armin/daxnuts 彩蛋、HTML 导出模板 |
| 启动升级通知 | `i18n/ui.json` 6 条：pi 新版本提示（标题 / 正文 / 更新日志行）+ 插件包更新提示（标题 / 正文 / 列表表头）；`pi update` 与 `pi update --extensions` 等 action 保持英文；服务端下发的 `release.note` 不汉化。本项目不提供自动升级（升级会整体替换包目录，使 A 线汉化失效、C/F 线 `source_version` 失配） |
| 插件简介汉化方式 | 运行时覆盖（`extensions/plugin-i18n.ts` + `i18n/plugins.json`），零改动插件源码 |
| 插件 UI 汉化方式 | 精确字面量补丁（`scripts/patch_plugin_ui.py` + `i18n/plugin-ui.json`），当前对象：`pi-powerline-footer@0.17.2` 的 `welcome.ts`（文案 18 条）与 `index.ts`（行为补丁 1 条）；跨文件原子预检 + `.zh-backup` 一键还原 + jiti 加载体检 |
| 已退役的行为改动①（2026-09-22） | `editor-chrome-thinking-border`（2026-09-12 授权）：powerline 0.17.2 已原生实现「editor 边框跟随 pi 思考层级色」（上游 #221：`colorEditorBorder()` 读取 `editor.borderColor`，保留灰色回退）；未打补丁跑 `python3 scripts/probe_editor_border.py` 已紫 6 / 灰 0 ⇒ 补丁退役，探针转为「上游是否回退」的回归哨兵 |
| 已授权的行为改动② | `welcome-header-eager-shell`（2026-09-13 授权）：启动欢迎页先用空数据挂 header 立即上屏，取数完成后再换 header 重绘（原先要等 `getRecentSessions` 扫盘 ~183ms 才上屏）。0.17.2 重锚：`discoverLoadedCounts()` 新增 `pi.getCommands()` 参数（上游 #218），逻辑未改 |
| 已退役的行为改动③（2026-09-22） | `dock-trim-primary-into-footer`（2026-09-13 授权）：powerline 0.17.2 已原生把次级详情放回 footer 预留行、footer 空时返回 `[]` 而非 `[""]`（上游 #217）；未打补丁跑 `python3 scripts/probe_dock_rows.py` 已「末行即状态行」 ⇒ 补丁退役，探针转为回归哨兵 |
| 配套用户配置 | `~/.pi/agent/settings.json` 置 `showLastPrompt: false`，关闭「↳ 上次输入」回显行（该开关由 `probe_dock_rows.py` 按配置判定；powerline 0.17.2 起 footer 空时不再占行，输入框下方净省 2 行） |
| 扩展环境懒人包 | `bundle/manifest.json`（E 线从本机白名单提取的声明）+ `scripts/install_bundle.sh`（D 线安装/回滚）；判据「**非默认值 + 非冲突解决 = 不带**」；导出单向（本机 SSOT → 仓库派生物） |
| 已授权行为改动④（**F 线首条**） | `todo-settle-nudge`（2026-09-15 授权）：`@juicesharp/rpiv-todo@2.10.1` 的 `tool/response-envelope.ts` —— 位于 `buildToolResult`，结算动作（`update`→completed/deleted、`delete`）后若列表仍有 `in_progress` 残留，则在工具返回里追加一行 `Unsettled:` 提醒（中文 subject 直接拼接，不翻译）。**回退分支**：非结算动作或无残留时恒等返回，行为与上游一致；整体还原走 `.zh-backup`（`--restore`）。验收：`python3 scripts/patch_plugin_ui.py --dict i18n/plugin-logic.json --check` 全命中 + 5 条路径行为实测（结算有/无残留、list、delete、create） |
| 已授权行为改动⑤（**F 线第二条**） | `todo-overlay-cleanup`（2026-09-17 授权）：`@juicesharp/rpiv-todo@2.10.1` 的 `todo-overlay.ts` —— 位于 `hideCompletedTasksFromPreviousTurn`，原实现只发非 forced `requestRender()`；隐藏后行数 N→0 属 shape change，而 widget 注销（`setWidget(key, undefined)`）只存在于 `update()` 内，于是全部结算后暗色 `○ Todos (n/n)` 滞留屏幕到下一次 todo 工具调用（2026-09-17 实测）。补丁把隐藏后的刷新交回 `update()`（空列表即走标准注销）并追加一次强制重绘。**回退分支**：仍有未完成任务时 `update()` 只做常规刷新 + 一次强制重绘，与上游可见行为一致；整体还原走 `.zh-backup`（`--restore`）。验收：`--check` 全命中 + `node scripts/probe_todo_overlay_cleanup.mjs` 三路径（触发 / 不触发 / 原版对照） |
| 懒人包内容规模 | 8 个包（含 `@narumitw/pi-btw`）+ 3 组协同配置（`quietStartup` / `tuiMode` / `powerline` 三段）+ 5 个文件（`pi-cc-extensions.json` + 4 个自研扩展）；`pi-cc-extensions.json` 走文件级字段白名单，只带 `showStartupHeader: false`（其余 20+ 字段均为插件默认值）；`npm/` 由 `packages` 清单重建 |
| 懒人包不带的 | `powerline.placement` / `powerline.cost.currency` / `showCacheMissNotices` / 模型三件套 / skill 与 Agent / 注入词，以及凭据、会话数据、个人路径等结构性排除项（见 `manifest.json` 的 `skip`） |
| 验收 | `AGENTS.md` §完成定义 五项 |

## 这卡解决什么

本项目的工作模式是**响应上游发版**：周期性、有明确起止。此前「上次适配到哪个版本、还有哪些条目没翻」只存在于提交信息里，需要翻 `git log` 才能重建。本卡让任何 AI 在 3 秒内接续工作。

2026-09-13 起增加第二条事务线：**扩展协同环境懒人包**（D 安装 / E 导出）。它与汉化线共享同一仓库，但触发条件不同——汉化跟随上游发版，懒人包跟随「本机协同配置变化」与「新机部署」。

## 六条独立的维护线

| 维护线 | 触发条件 | 入口 | 是否需重打补丁 |
| :--- | :--- | :--- | :--- |
| A. Pi 本体汉化 | 上游发新版 | `scripts/apply_patch.sh` | 是（跟随版本号） |
| B. 插件简介汉化 | 插件升级 / 新增插件 | `scripts/scan_plugin_commands.py --check` | 否（运行时覆盖，只增量补字典） |
| C. 插件 UI 汉化 | 插件升级 / 改了渲染文案 | `scripts/apply_plugin_ui.sh --check` | 是（补丁式，幂等重打；基底始终取自 `.zh-backup`） |
| D. 扩展环境安装 | 新机器 / 想装这套协同环境 | `scripts/install_bundle.sh` | 否（幂等；按 `bundle/manifest.json` 声明装） |
| E. 扩展环境导出 | 本机改了自研扩展或协同配置 | `scripts/export_bundle.sh` | 否（单向导出；`check_bundle.sh` 做漂移检测） |
| F. 插件功能补丁 | 上游插件改段 / 新增授权补丁 | `python3 scripts/patch_plugin_ui.py --dict i18n/plugin-logic.json --check` | 是（补丁式，幂等重打；基底始终取自 `.zh-backup`） |

> C 线是唯一会写 `~/.pi/agent/npm/node_modules` 的汉化线，仅限“无注册接口可拦截的渲染期硬编码文案”。
> 它不被插件命令简介（B 线）覆盖，也不使 pi 本体汉化（A 线）失效；插件升级后只需重跑一条命令。
>
> D / E 线只管「扩展协同环境」（扩展清单 + 协同配置 + 自研扩展），不管汉化本体：
> 汉化随仓库的 A/B/C 线分发，D 线装完后依次执行三条汉化线即可得到完整效果。
