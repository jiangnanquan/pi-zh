---
status: 持续运行
status_since: 2026-09-06
owner: "jnq & AI"
entry: "SOP.md"
summary: "Pi CLI 汉化维护：适配 v0.85.1，i18n/ 六源共 163 条翻译事实（CLI 128 + 插件命令 35），含 DoD 四项冒烟验收"
next: "跟踪上游 @earendil-works/pi-coding-agent 发版；插件升级后跑 scripts/scan_plugin_commands.py --check 增量补译"
updated: 2026-09-12
---

> 本项目采用 PKM 时空胶囊方法论（**零工具档：仅约定，未引入 task_status.py**）。
> 本卡记录维护基线；操作规程见 `SOP.md`，维护红线与完成定义见 `AGENTS.md`。
>
> 因未引入工具，`source_hash` 字段省略 —— 它由 `task_status.py` 计算，属"约定 + 工具"档位的能力。
> 建立：`qPKM治理` @ 2026-09-11

## 当前基线

| 项 | 值 |
| :--- | :--- |
| 适配版本 | Pi `0.85.1`（`scripts/patch_engine.py` → `SUPPORTED_VERSIONS`） |
| 翻译事实源 | `i18n/` 六源：`cli.json` / `commands.json` / `keybindings.json` / `settings.json` / `ui.json` / `plugins.json` |
| 条目规模 | 共 163 条（CLI 汉化 128：keybindings 62 · settings 37 · commands 23 · cli 3 · ui 3；插件命令简介 35） |
| 插件汉化方式 | 运行时覆盖（`extensions/plugin-i18n.ts` + `i18n/plugins.json`），零改动插件源码 |
| 验收 | `AGENTS.md` §完成定义 五项 |

## 这卡解决什么

本项目的工作模式是**响应上游发版**：周期性、有明确起止。此前「上次适配到哪个版本、还有哪些条目没翻」只存在于提交信息里，需要翻 `git log` 才能重建。本卡让任何 AI 在 3 秒内接续工作。

## 两条独立的维护线

| 维护线 | 触发条件 | 入口 | 是否需重打补丁 |
| :--- | :--- | :--- | :--- |
| Pi 本体汉化 | 上游发新版 | `scripts/apply_patch.sh` | 是（跟随版本号） |
| 插件简介汉化 | 插件升级 / 新增插件 | `scripts/scan_plugin_commands.py --check` | 否（运行时覆盖，只增量补字典） |
