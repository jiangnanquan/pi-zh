---
status: 持续运行
status_since: 2026-09-06
owner: "jnq & AI"
entry: "SOP.md"
summary: "Pi CLI 汉化维护：适配 v0.85.1，i18n/ 七源共 181 条翻译事实（CLI 128 + 插件命令 34 + 插件 UI 19）+ 3 条用户授权行为补丁（7 个锚点）：editor 边框跟随思考色 / 欢迎页空壳先行 / dock 去空白占位行；六条维护线（A 本体 / B 简介 / C 渲染文案 / D 懒人包安装 / E 懒人包导出 / F 插件功能补丁，F1=todo 结算残留提醒）；含 DoD 五项冒烟验收 + F 线四步验收"
next: "跟踪上游发版；插件升级后跑 scripts/scan_plugin_commands.py --check 补译命令简介，跑 scripts/apply_plugin_ui.sh --check 审查欢迎页文案与行为补丁漂移，跑 scripts/patch_plugin_ui.py --dict i18n/plugin-logic.json --check 审查 F 线功能补丁漂移，并用 scripts/probe_editor_border.py + scripts/probe_dock_rows.py 复验两条渲染行为；本机自研扩展或协同配置有变动时跑 scripts/export_bundle.sh 重新导出并跑 check_bundle.sh 确认无漂移"
updated: 2026-09-15
---

> 本项目采用 PKM 时空胶囊方法论（**零工具档：仅约定，未引入 task_status.py**）。
> 本卡记录维护基线；操作规程见 `SOP.md`，维护红线与完成定义见 `AGENTS.md`。
>
> 因未引入工具，`source_hash` 字段省略 —— 它由 `task_status.py` 计算，属"约定 + 工具"档位的能力。
> 建立：`qPKM治理` @ 2026-09-11

## 当前基线

| 项 | 值 |
| :--- | :--- |
| 适配版本 | Pi `0.85.1`（`scripts/patch_engine.py` → `SUPPORTED_VERSIONS`） |
| 翻译事实源 | `i18n/` 七源：`cli.json` / `commands.json` / `keybindings.json` / `settings.json` / `ui.json` / `plugins.json` / `plugin-ui.json` |
| 条目规模 | 共 181 条翻译事实（CLI 汉化 128：keybindings 62 · settings 37 · commands 23 · cli 3 · ui 3；插件命令简介 34；插件 UI 文案 19），另有 3 条用户授权行为补丁（共 7 个锚点） |
| 插件简介汉化方式 | 运行时覆盖（`extensions/plugin-i18n.ts` + `i18n/plugins.json`），零改动插件源码 |
| 插件 UI 汉化方式 | 精确字面量补丁（`scripts/patch_plugin_ui.py` + `i18n/plugin-ui.json`），当前对象：`pi-powerline-footer@0.17.1` 的 `welcome.ts`（文案）与 `index.ts`（行为补丁）；跨文件原子预检 + `.zh-backup` 一键还原 + jiti 加载体检 |
| 已授权的行为改动① | `editor-chrome-thinking-border`（2026-09-12 由用户拍板）：powerline 重画的 editor 上下边框从硬编码 ANSI 244 灰改为继承 pi 注入的 `borderColor`（跟随思考层级色），保留灰色回退分支；行为验收：`python3 scripts/probe_editor_border.py`（未打补丁 editor 宽 紫 2/灰 4 → 已打补丁 紫 6/灰 0） |
| 已授权的行为改动② | `welcome-header-eager-shell`（2026-09-13 授权）：启动欢迎页先用空数据挂 header 立即上屏，取数完成后再换 header 重绘（原先要等 `getRecentSessions` 扫盘 ~183ms 才上屏） |
| 已授权的行为改动③ | `dock-trim-primary-into-footer`（2026-09-13 授权）：`placement=below` 时主状态行改由 footer 槽位渲染，消掉 pi dock 给空壳 footer 保底的那一行空白；widget 在该模式下返回空数组避免重复渲染，`placement=above` 保留原空壳行为。行为验收：`python3 scripts/probe_dock_rows.py`（未打补丁：状态行之后仍有 1 行空壳占位 → 已打补丁：屏幕最后一行即主状态行） |
| 配套用户配置 | `~/.pi/agent/settings.json` 置 `showLastPrompt: false`，关闭「↳ 上次输入」回显行（与行为改动③合计收回输入框下方 2 行；该开关由 `probe_dock_rows.py` 按配置判定） |
| 扩展环境懒人包 | `bundle/manifest.json`（E 线从本机白名单提取的声明）+ `scripts/install_bundle.sh`（D 线安装/回滚）；判据「**非默认值 + 非冲突解决 = 不带**」；导出单向（本机 SSOT → 仓库派生物） |
| 已授权行为改动④（**F 线首条**） | `todo-settle-nudge`（2026-09-15 授权）：`@juicesharp/rpiv-todo@2.10.1` 的 `tool/response-envelope.ts` —— 位于 `buildToolResult`，结算动作（`update`→completed/deleted、`delete`）后若列表仍有 `in_progress` 残留，则在工具返回里追加一行 `Unsettled:` 提醒（中文 subject 直接拼接，不翻译）。**回退分支**：非结算动作或无残留时恒等返回，行为与上游一致；整体还原走 `.zh-backup`（`--restore`）。验收：`python3 scripts/patch_plugin_ui.py --dict i18n/plugin-logic.json --check` 全命中 + 5 条路径行为实测（结算有/无残留、list、delete、create） |
| 懒人包内容规模 | 7 个包 + 3 组协同配置（`quietStartup` / `tuiMode` / `powerline` 三段）+ 5 个文件（`claude-code-style.json` + 4 个自研扩展），共约 60KB；`npm/` 的 21,640 个文件由 `packages` 清单重建 |
| 懒人包不带的 | `powerline.placement` / `powerline.cost.currency` / `showCacheMissNotices` / 模型三件套 / skill 与 Agent / 注入词，以及凭据、会话数据、个人路径等结构性排除项（见 `manifest.json` 的 `skip`） |
| 验收 | `AGENTS.md` §完成定义 五项 |

## 这卡解决什么

本项目的工作模式是**响应上游发版**：周期性、有明确起止。此前「上次适配到哪个版本、还有哪些条目没翻」只存在于提交信息里，需要翻 `git log` 才能重建。本卡让任何 AI 在 3 秒内接续工作。

2026-09-13 起增加第二条事务线：**扩展协同环境懒人包**（D 安装 / E 导出）。它与汉化线共享同一仓库，但触发条件不同——汉化跟随上游发版，懒人包跟随「本机协同配置变化」与「新机部署」。

## 五条独立的维护线

| 维护线 | 触发条件 | 入口 | 是否需重打补丁 |
| :--- | :--- | :--- | :--- |
| A. Pi 本体汉化 | 上游发新版 | `scripts/apply_patch.sh` | 是（跟随版本号） |
| B. 插件简介汉化 | 插件升级 / 新增插件 | `scripts/scan_plugin_commands.py --check` | 否（运行时覆盖，只增量补字典） |
| C. 插件 UI 汉化 | 插件升级 / 改了渲染文案 | `scripts/apply_plugin_ui.sh --check` | 是（补丁式，幂等重打；基底始终取自 `.zh-backup`） |
| D. 扩展环境安装 | 新机器 / 想装这套协同环境 | `scripts/install_bundle.sh` | 否（幂等；按 `bundle/manifest.json` 声明装） |
| E. 扩展环境导出 | 本机改了自研扩展或协同配置 | `scripts/export_bundle.sh` | 否（单向导出；`check_bundle.sh` 做漂移检测） |

> C 线是唯一会写 `~/.pi/agent/npm/node_modules` 的汉化线，仅限“无注册接口可拦截的渲染期硬编码文案”。
> 它不被插件命令简介（B 线）覆盖，也不使 pi 本体汉化（A 线）失效；插件升级后只需重跑一条命令。
>
> D / E 线只管「扩展协同环境」（扩展清单 + 协同配置 + 自研扩展），不管汉化本体：
> 汉化随仓库的 A/B/C 线分发，D 线装完后依次执行三条汉化线即可得到完整效果。
